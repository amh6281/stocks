import { Button } from '@/components/ui/button';

const Home = () => {
  return (
    <div className='flex h-screen items-center justify-center'>
      <Button>test</Button>
    </div>
  );
};

export default Home;
